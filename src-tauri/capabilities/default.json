{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Core window functions; journal and selected-folder operations use narrow native commands.",
  "windows": ["main"],
  "permissions": ["core:default", "core:window:allow-destroy"]
}
